# Task Packet: Fix SQLiteLibraryRepositoryTests LibraryDatabase lifetime bug

**Packet ID:** 04-002.5-sqlite-test-cleanup
**Phase:** 4 (debt fix)
**Created:** 2026-05-18
**Status:** READY
**Auto-proceed:** true
**High-risk:** false

## Goal

`SQLiteLibraryRepositoryTests.makeRepo()` creates a `LibraryDatabase` as a local that can
be ARC-released before async test operations run — the same bug fixed in
`DownloadRepositoryTests` during `04-002`. Apply the identical `(repo, db)` tuple pattern
to keep the database alive for the test's lifetime.

## Acceptance criteria

- [ ] `SQLiteLibraryRepositoryTests.makeRepo()` returns
      `(SQLiteLibraryRepository, LibraryDatabase)` — same pattern as
      `DownloadRepositoryTests.makeRepo()`.
- [ ] Every test that calls `makeRepo()` destructures as `let (repo, _db) = makeRepo()`.
- [ ] All Swift tests pass.

## Read first

- `TapeScrapeTests/Repositories/SQLiteLibraryRepositoryTests.swift` — the file to fix
- `TapeScrapeTests/Repositories/DownloadRepositoryTests.swift` lines 121–126 — the
  pattern to mirror

## Files expected to change

- `TapeScrapeTests/Repositories/SQLiteLibraryRepositoryTests.swift` — `makeRepo()`
  return type + every call site

## Tests

- REQUIRED — this packet IS the test fix. All Swift tests green after.

## Out of scope

Everything except the `makeRepo()` lifetime fix.

## Summary output path

`workflow/packets/04-002.5-sqlite-test-cleanup.summary.md`
